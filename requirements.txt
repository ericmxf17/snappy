anthropic
# 13.x replaced the constructor with a required `auth=` API and crashes Snappy during
# packaged startup. Pin the SDK contract the wrapper is implemented and tested against.
snaptrade-python-sdk==11.0.212

# Whisper on the CTranslate2 runtime instead of PyTorch. Same model, same accuracy,
# ~4x faster on CPU — and it drops a 491 MB dependency. (openai-whisper pulled in
# torch, numba, llvmlite and sympy: 810 MB of venv for a 74M-parameter model.)
faster-whisper

sounddevice
soundfile
numpy

# macOS UI. rumps only depends on pyobjc-framework-cocoa, so the rest are listed
# explicitly — WebKit in particular was only ever installed by accident, and a
# fresh clone would have died on `import WebKit`.
rumps
pyobjc-framework-Cocoa
pyobjc-framework-WebKit
pyobjc-framework-Quartz
pyobjc-framework-ApplicationServices  # AXIsProcessTrusted — the ⌥ hotkey needs Accessibility

python-dotenv
requests
websockets

# Tests. They run offline with no credentials — nothing here reaches the network.
pytest
fastapi
uvicorn[standard]

# macOS packaging. Keep this explicit: setup_requires tries to invoke `pip` from
# inside uv-created environments, which intentionally may not include pip.
py2app==0.28.10
